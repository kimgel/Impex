'use strict';

define(['app'], function(app) {
    app.controller('ShippingForwarderList', [
        '$scope',
        '$location',
        function($scope, $location) {
            
        }
    ]);
});
