'use strict';

define(['app'], function(app) {
    app.controller('AssignForwarderList', [
        '$scope',
        '$location',
        function($scope, $location) {
            
        }
    ]);
});
