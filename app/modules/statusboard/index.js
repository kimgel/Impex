'use strict';

define(['app'], function(app) {
    app.controller('DashboardCtrl', [
        '$scope',
        '$location',
        'Auth',
        function($scope, $location, Auth) {
        	
        }
    ]);
});
